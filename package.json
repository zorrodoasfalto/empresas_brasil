{
  "name": "empresas-brasil-production-final",
  "version": "5.6.1",
  "description": "API para consulta de empresas brasileiras - PRODUCTION READY",
  "main": "backend/run-server.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps && npm run build",
    "start": "node backend/run-server.js",
    "dev": "node backend/run-server.js"
  },
  "keywords": [
    "empresas",
    "brasil",
    "cnpj",
    "consulta",
    "nodejs",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
